# ADA Data

This is ADA-owned persistent state. `characters`, `missions`, `library`, `runs`, and `references` are authoritative. `indexes` is rebuildable. `tmp` is disposable and must never contain the only copy of an asset or receipt.
